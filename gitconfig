[user]
	name = Joe Fredette
	email = jfredett@gmail.com

[color]
  diff = auto
  status = auto
  branch = auto
  interactive = auto

[alias]
  st = status -s
  ci = commit
  ca = commit -a
  co = checkout
  w = whatchanged
  pm = push -u origin master
  core = shortlog -s -n --no-merges
  rc = rebase --continue
  cont = rebase --continue
  abort = rebase --abort
  skip = rebase --skip
  gr="log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset) - %C(blue)Sig:%G?' --all"
  patch= commit -S --amend --no-edit

[core]
  excludesfile = /Users/jfredett/.gitignore_global

[heroku]
  account = personal

[push]
  default = current

[rerere]
  enabled = true

[include]
  path = ~/.bash/github_gitconfig
